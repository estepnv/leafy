sudo: false

language: ruby

rvm:
  - 2.2
  - 2.3
  - 2.4
  - 2.5
  - ruby-head
  - jruby-9.2.0.0
  - jruby-head

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head

before_install: gem install bundler

script: bundle exec rspec

cache: bundler

env:
  - RACK_ENV=development
